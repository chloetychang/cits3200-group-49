# Check that connection to postgres from fastAPI works. 
# Check that Interaction between frontend and backend works (flutter submits/requests data from fastAPI, visible on database or flutter). 
# Check that speed of interactions is acceptable. 